# Dependencies
node_modules/

# Build output
dist/
src-tauri/target/

# Environment / secrets
.env
.env.*

# Tauri signing keys (never commit)
*.key
*.key.pub

# Large bundled binaries (download via CI/setup script instead)
src-tauri/binaries/*.exe
src-tauri/binaries/*.dll

# OS
.DS_Store
Thumbs.db
